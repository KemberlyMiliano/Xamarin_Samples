<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppXample.DiscoveryButtonView"
             x:Name ="discoveryButtonView">
    <ContentView.Content>

        <StackLayout Margin ="8" BindingContext="{x:Reference discoveryButtonView}">

            <Image Source="{Binding IconImageSource}"  HeightRequest="80" Margin="0,10,0,0"/>

            <Label FontSize="18"
                FontAttributes="Bold"
                TextColor="Black"
                HorizontalOptions="CenterAndExpand"
                Text="{Binding Label1_Text}"/>

            <Label FontSize="13"
                HorizontalOptions="Center"
                Text="{Binding Label2_Text}"/>

        </StackLayout>

    </ContentView.Content>
</ContentView>